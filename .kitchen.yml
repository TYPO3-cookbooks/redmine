---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: debian-8.2

suites:
- name: default
  run_list:
    - recipe[redmine::default]
  attributes:
    mysql:
      server_root_password: linux4evr
    redmine:
      database:
        username: redmine
        password: notforproduction
